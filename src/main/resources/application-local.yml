spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/test
        username: test
        password: test
    jpa:
        hibernate:
            ddl-auto: update

custom:
    rest-service:
        services:
            default:
                ttl: 0
                retries: 0
            percentage:
                ttl: 30
                retries: 3
